/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   serverActions: true,
  // },
  // basePath, rewrites, dll kalau ada
};

export default nextConfig;
