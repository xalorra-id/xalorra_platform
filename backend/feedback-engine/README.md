# Xalorra Labeling GPT

API sederhana untuk labeling data teks menggunakan OpenAI GPT.

## Setup

1. Buat virtual environment dan install dependencies

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
